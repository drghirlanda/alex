data <- alex.read( "../../Examples/ExternalInhibition/" )
data <- alex.read( "../../Examples/ExternalInhibition/" )
str(data)
data <- subset( data,  Phase != "Instr" )
str(data)
data <- droplevels( subset( data,  Phase != "Instr" ) )
str(data)
pres <- alex.presentation( data )
n.subjects
phases
p <- "Training"
my.phase
unique( data$Stimulus )
s <- "A"
my.cs
data$Trial
my.trials <- data$Trial[ my.phase & my.cs ]
my.trials
cases <- my.phase & my.cs & data$Trial == 2
cases
pres[ cases ]
pres[ cases ] <- 1
pres <- alex.presentation( data )
pres
data$Presentation <- alex.presentation( data )
data[ , c("Stimulus", "Presentation") ]
data[ , c("Phase", "Stimulus", "Presentation") ]
data <- alex.read( "../../Examples/ExternalInhibition/" )
str(data)
str(data)
data <- alex.read( "../../Examples/ExternalInhibition/" )
str(data)
help(strsplit)
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
strsplit( "Training|A|1", "|" )
strsplit( "Training|A|1", "|", fixed=T )
h <- alex.rthist( data, "Training|A|1" )
subset( data, Phase == "Training" )
subset( data, Phase == "Training" & Stimulus == "A" )
subset( data, Phase == "Training" & Stimulus == "A" & ResponseCount == "1" )
data$RT
subset( data, Phase == "Training" & Stimulus == "A" & ResponseCount == "1" )$RT
hist( subset( data, Phase == "Training" & Stimulus == "A" & ResponseCount == "1" )$RT )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
data$ResponseCount
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
strsplit( "A|B|C", "|", fixed=T )
strsplit( "A|B|C", "|", fixed=T )[1]
strsplit( "A|B|C", "|", fixed=T )[1][1]
strsplit( "A|B|C", "|", fixed=T )$1
strsplit( "A|B|C", "|", fixed=T )$"1"
a <- strsplit( "A|B|C", "|", fixed=T )
a
as.vector( a )
t(a)
a[1]
a[[1]]
a[[1]][1]
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training|A|1" )
h <- alex.rthist( data, "Training:A:1" )
plot( RT ~ Presentation, data )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A" )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A", type="l", ylim=c(0,2000) )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A", type="l", ylim=c(0,1000) )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="X", type="l", ylim=c(0,1000) )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="X", type="b", ylim=c(0,1000) )
plot( RT ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A", type="b", ylim=c(0,1000) )
plot( ResponseCount ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A", type="b", ylim=c(0,1000) )
plot( ResponseCount ~ Presentation, data, subset=Phase=="Training" & Stimulus=="A", type="b", ylim=c(0,5) )
h <- alex.hist( data, "RT", "Training:A:1" )
h <- alex.hist( data, "RT", "Training:A:1" )
h
h <- alex.hist( data, "RT", "Training:A:*" )
h <- alex.hist( data, "RT", "Training:A:2" )> 
)
h <- alex.hist( data, "RT", "Training:A:2" )
h <- alex.hist( data, "RT", "Training:A:2" )
h <- alex.hist( data, "RT", "Training:A:2" )
h <- alex.hist( data, "RT", "Training:A:2" )
quit()
n
